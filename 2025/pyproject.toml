[project]
name = "2025"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "kivy>=2.3.1",
    "pytest>=9.0.1",
    "python-dotenv>=1.2.1",
]
